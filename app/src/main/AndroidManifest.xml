<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="allam9072.mealplanner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TempActivity"></activity>
        <activity
            android:name=".ui.ListActivity.ListActivity"
            android:label="@string/title_activity_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.view_days.DayViewActivity" />
        <activity android:name=".ui.view_weeks.WeeksViewActivity" />
        <activity android:name=".ui.view_product_profile.ProductProfileActivity" />
        <activity
            android:name=".ui.add_meal.MealPlanActivity"
            android:parentActivityName=".ui.add_day.DayPlanActivity" />
        <activity android:name=".ui.add_day.DayPlanActivity" />
        <activity android:name=".ui.add_week.WeekActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>